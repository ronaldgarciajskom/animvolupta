if (typeof module !== 'undefined' && module.exports) {
  // Code that runs in a Node.js environment
  // Exporting functions or objects using module.exports
} else {
  // Code that runs in a browser or non-Node.js environment
  // Handle the absence of module.exports
}
